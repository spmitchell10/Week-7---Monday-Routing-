(function() {
    'use strict';

    angular
        .module('routing')
        .controller('MainControllers', function(API) {
        	const vm = this;
            vm.name = "Stephen";
        });
     
})();